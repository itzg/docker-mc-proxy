version: 2
updates:
  # Maintain dependencies for GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      docker:
        patterns:
          - "docker/*"
      updates:
        patterns:
          - "*"
        update-types:
          - patch
          - minor
